<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10 flex gap-3">
            <a class="text-blue-500" href="/">Home</a>
            <a class="text-blue-500" href="/read">Read Users</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3">Update User</h1>
        <form action="/update/<%= user._id %>" method="post">
            <input class="px-5 py-2 border-2 border-zinc-800 bg-zinc-500 rounded-lg " type="text" value="<%= user.name%>" placeholder="Name" name="name" >
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-lg bg-zinc-500" type="email"  value="<%= user.email %>" placeholder="Email" name="email">
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-lg bg-zinc-500" type="text" value="<%= user.image %>" placeholder="Image url" name="image">
            <input class="px-5 py-2 bg-blue-500 rounded-lg" type="submit" value="Update User">
        </form>
    </div>
</body>
</html>